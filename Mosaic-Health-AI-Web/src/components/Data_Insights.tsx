import { Card, Typography } from '@mui/joy';

export default function Data_Insights() {

    return (
        <Card color='primary' sx={{ margin: 2, justifyContent: 'center', flexGrow: 2 }}>
            <Typography> Data Insights </Typography>
        </Card >
    );
}